package main

import "fmt"

func main() {
	var username string = "Hari"
	fmt.Println(username)
	fmt.Printf("Variable is the type: %T \n", username)

	var isLoggedIn bool = false
	fmt.Println(isLoggedIn)
	fmt.Printf("Variable is the type: %T \n", isLoggedIn)
}
